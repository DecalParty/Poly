# Polymarket API Configuration
# Your Ethereum private key (for signing orders)
PRIVATE_KEY=0x_your_private_key_here

# Your Polymarket profile/funder address (where USDC is deposited)
FUNDER_ADDRESS=0x_your_funder_address_here

# Polygon RPC endpoint (use a dedicated provider for low latency)
# Free: https://polygon-rpc.com (slow, ~200ms)
# Fast: Alchemy, QuickNode, or Infura with EU region endpoint
# Example: https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY
POLYGON_RPC_URL=https://polygon-rpc.com

# Polymarket CLOB API host
CLOB_API_URL=https://clob.polymarket.com

# Polymarket Gamma API host (for market data)
GAMMA_API_URL=https://gamma-api.polymarket.com

# Chain ID (137 = Polygon mainnet)
CHAIN_ID=137

# Signature type (0 = EOA/MetaMask, 1 = Magic.link)
SIGNATURE_TYPE=0

# CLOB API Keys (optional — auto-generated if not set)
# Run "node generate-keys.js" on your local PC to get these.
# Required for VPS deployments where Polymarket blocks key generation.
# CLOB_API_KEY=
# CLOB_SECRET=
# CLOB_PASSPHRASE=

# Logging
LOG_LEVEL=info
LOG_FILE=./data/bot.log

# Binance WebSocket region (auto-detected if not set)
# "us" = Binance.US (for US IPs), "intl" = Binance International (for non-US VPS)
# BINANCE_REGION=auto

# Brave Search API key (for Copilot MCP web search)
BRAVE_API_KEY=your_brave_api_key_here
